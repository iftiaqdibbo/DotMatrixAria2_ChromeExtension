src/components/aria2-download-row.ts | 44 | high | The `tick` closure in `_startSmooth` captures the `d` parameter from its first invocation. But `willUpdate` replaces `this.download` with a new object on every poll (dashboard.ts:111 creates fresh objects each poll). On subsequent polls of an active download, lines 67-69 update `_lastCompleted`/`_lastSpeed`/`_lastUpdate` on the instance, but line 70 skips restarting the RAF (`!_rafId` is false). The running `tick` still reads stale `d.totalLength` and `d.status` from the closure. If `totalLength` changes mid-download (e.g. discovered from 0 to a real value), the denominator is wrong; if status changes to complete, RAF doesn't self-stop. | Stale closure capture misses updated download object | Make `tick` read `this.download` instead of the captured closure `d`, or remove the `!_rafId` guard on line 70 so `_startSmooth(this.download)` is called on every `willUpdate` for active downloads (which re-captures the fresh object).